// Admin settings entry — compiled to admin/js/settings.js
const { createRoot, render } = wp.element || {};

// Basic admin settings dashboard placeholder
document.addEventListener('DOMContentLoaded', function() {
	const container = document.getElementById('bpar-admin-root');
	if (!container) return;

	// Server-rendered settings page; this file handles progressive enhancement
	console.log('BePlus Advanced Reviews admin initialized');
});
